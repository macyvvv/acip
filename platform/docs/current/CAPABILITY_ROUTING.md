# CAPABILITY_ROUTING

Worker Registry is the single source of truth for worker selection.
Capability routing uses worker capability, allowed actions, prohibited actions, and validation responsibility.
Deterministic tie-breaking prefers the highest score, then lexicographic worker name.
